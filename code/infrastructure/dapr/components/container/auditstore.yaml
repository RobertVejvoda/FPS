apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: auditstore
spec:
  type: state.mongodb
  version: v1
  metadata:
  - name: host
    value: mongodb:27017
  - name: databaseName
    value: fairspot-audit
  - name: collectionName
    value: auditlog
  - name: username
    secretKeyRef:
      name: mongodb-credentials
      key: username
  - name: password
    secretKeyRef:
      name: mongodb-credentials
      key: password
  - name: authSource
    value: admin
  - name: params
    value: "?authSource=admin&ssl=false"
scopes:
- fairspot-audit
auth:
  secretStore: secretstore
