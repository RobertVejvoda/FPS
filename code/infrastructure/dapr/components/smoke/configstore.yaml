apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: configstore
spec:
  type: state.in-memory
  version: v1
scopes:
- fps-configuration
# Smoke-test only. State is not persisted across restarts.
