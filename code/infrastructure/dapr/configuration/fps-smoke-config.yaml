apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: dapr-smoke-configuration
spec:
  actors:
    actorStateStore: workflowstore
  mtls:
    enabled: false
    # Local smoke keeps mTLS off, so workflow history signing must stay disabled.
