# Image-only FairSpot service definitions for the NAS / pulled-image profile.
#
# This file has NO build sections — every service runs a pre-built image pulled
# from a registry (GHCR by default). The NAS host therefore needs no source build
# context, .NET SDK, Node, npm, or Dapr CLI.
#
# Use this INSTEAD of docker-compose.services.yml for the NAS profile:
#   docker compose --env-file nas.env \
#     -f docker-compose.yaml \
#     -f docker-compose.services.images.yml \
#     -f docker-compose.dapr.yml \
#     -f docker-compose.nas.yml \
#     -f docker-compose.services.nas.yml \
#     pull
#   docker compose --env-file nas.env \
#     -f docker-compose.yaml \
#     -f docker-compose.services.images.yml \
#     -f docker-compose.dapr.yml \
#     -f docker-compose.nas.yml \
#     -f docker-compose.services.nas.yml \
#     up -d
#
# Image selection (override in .env):
#   FPS_REGISTRY    registry + owner prefix. Default: ghcr.io/robertvejvoda
#   FPS_IMAGE_TAG   image tag. Default: latest. Pin a sha-<commit> for repeatable
#                   deploys and easy rollback (see the NAS deployment runbook).

x-fairspot-service-auth-env: &fairspot-service-auth-env
  Auth__Authority: ${FPS_AUTH_AUTHORITY:-http://keycloak:8080/realms/fps-local}
  Auth__Audience: ${FPS_AUTH_AUDIENCE:-fps-mobile-dev}
  Auth__AdditionalAudiences: ${FPS_AUTH_ADDITIONAL_AUDIENCES:-fps-web-dev,fps-cli-dev}
  Auth__AllowHttpMetadata: ${FPS_AUTH_ALLOW_HTTP_METADATA:-true}
  Auth__AllowLocalIssuerHostOverride: ${FPS_AUTH_ALLOW_LOCAL_ISSUER_HOST_OVERRIDE:-true}
  # PLAT001: the FairSpot-controlled realm's privileged roles may pass through for tenants
  # not yet explicitly mapped (the shared ConfiguredTenantRoleMapper strips privileged roles
  # otherwise). Clear/override this for a brokered-customer deployment that maps roles per tenant.
  Auth__TrustedRealmRoles: ${FPS_AUTH_TRUSTED_REALM_ROLES:-admin,hr_manager,auditor,report_viewer}
  # PLAT001 platform plane: setting a platform realm authority makes ConfigureFpsJwtBearer accept
  # BOTH the customer realm and the platform realm, and confines platform_* roles to the platform
  # issuer (TenantClaimsTransformation). Empty by default → platform plane dormant. Set these to
  # deploy platform-plane surfaces (e.g. the DataHub usage-stats API, the Customer tenant console).
  Auth__PlatformAuthority: ${FPS_AUTH_PLATFORM_AUTHORITY:-}
  Auth__PlatformIssuer: ${FPS_AUTH_PLATFORM_ISSUER:-}

services:

  fairspot-audit:
    image: ${FPS_REGISTRY:-ghcr.io/robertvejvoda}/fairspot-audit:${FPS_IMAGE_TAG:-latest}
    ports:
      - "5161:5161"
    environment:
      <<: *fairspot-service-auth-env
      ASPNETCORE_URLS: "http://+:5161"
      ASPNETCORE_ENVIRONMENT: ${FPS_ASPNETCORE_ENVIRONMENT:-Production}
    networks:
      - web

  fairspot-booking:
    image: ${FPS_REGISTRY:-ghcr.io/robertvejvoda}/fairspot-booking:${FPS_IMAGE_TAG:-latest}
    ports:
      - "5131:5131"
    environment:
      <<: *fairspot-service-auth-env
      ASPNETCORE_URLS: "http://+:5131"
      ASPNETCORE_ENVIRONMENT: ${FPS_ASPNETCORE_ENVIRONMENT:-Production}
      ProfileService__BaseUrl: "http://fairspot-profile:5197"
    networks:
      - web

  fairspot-configuration:
    image: ${FPS_REGISTRY:-ghcr.io/robertvejvoda}/fairspot-configuration:${FPS_IMAGE_TAG:-latest}
    ports:
      - "5141:5141"
    environment:
      <<: *fairspot-service-auth-env
      ASPNETCORE_URLS: "http://+:5141"
      ASPNETCORE_ENVIRONMENT: ${FPS_ASPNETCORE_ENVIRONMENT:-Production}
    networks:
      - web

  fairspot-customer:
    image: ${FPS_REGISTRY:-ghcr.io/robertvejvoda}/fairspot-customer:${FPS_IMAGE_TAG:-latest}
    ports:
      - "5181:5181"
    environment:
      <<: *fairspot-service-auth-env
      ASPNETCORE_URLS: "http://+:5181"
      ASPNETCORE_ENVIRONMENT: ${FPS_ASPNETCORE_ENVIRONMENT:-Production}
      DemoSeed__ProfileBaseUrl: "http://fairspot-profile:5197"
      DemoSeed__ConfigurationBaseUrl: "http://fairspot-configuration:5141"
      # PLAT003C-C3: sandbox-reset activation for the hosted/NAS demo. OFF by default (fail closed);
      # set FPS_SANDBOX_RESET_ENABLED=true in the hosted env only for a Green Logistics demo profile.
      # The reset refuses any tenant that is not a stored resettable sandbox. The scheduler flag arms
      # the nightly sandbox-reset-scheduler cron tick (a safe no-op while Enabled is false).
      SandboxReset__Enabled: ${FPS_SANDBOX_RESET_ENABLED:-false}
      SandboxReset__Scheduler__Enabled: ${FPS_SANDBOX_RESET_SCHEDULER_ENABLED:-false}
      Readiness__ProfileBaseUrl: "http://fairspot-profile:5197"
      Readiness__BookingBaseUrl: "http://fairspot-booking:5131"
      Readiness__NotificationBaseUrl: "http://fairspot-notification:5157"
      Readiness__AuditBaseUrl: "http://fairspot-audit:5161"
      Readiness__ReportingBaseUrl: "http://fairspot-reporting:5171"
    networks:
      - web

  fairspot-datahub:
    image: ${FPS_REGISTRY:-ghcr.io/robertvejvoda}/fairspot-datahub:${FPS_IMAGE_TAG:-latest}
    ports:
      - "5211:5211"
    environment:
      <<: *fairspot-service-auth-env
      ASPNETCORE_URLS: "http://+:5211"
      ASPNETCORE_ENVIRONMENT: ${FPS_ASPNETCORE_ENVIRONMENT:-Production}
      ConnectionStrings__DataHub: "Host=postgres;Port=5432;Database=${POSTGRES_DB:-fps_datahub};Username=${POSTGRES_USER:-fps};Password=${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required for production-like DataHub, see nas.env or run via start-container-stack.sh}"
    networks:
      - web

  fairspot-identity:
    image: ${FPS_REGISTRY:-ghcr.io/robertvejvoda}/fairspot-identity:${FPS_IMAGE_TAG:-latest}
    ports:
      - "5192:5192"
    environment:
      <<: *fairspot-service-auth-env
      ASPNETCORE_URLS: "http://+:5192"
      ASPNETCORE_ENVIRONMENT: ${FPS_ASPNETCORE_ENVIRONMENT:-Production}
    networks:
      - web

  fairspot-notification:
    image: ${FPS_REGISTRY:-ghcr.io/robertvejvoda}/fairspot-notification:${FPS_IMAGE_TAG:-latest}
    ports:
      - "5157:5157"
    environment:
      <<: *fairspot-service-auth-env
      ASPNETCORE_URLS: "http://+:5157"
      ASPNETCORE_ENVIRONMENT: ${FPS_ASPNETCORE_ENVIRONMENT:-Production}
    networks:
      - web

  fairspot-profile:
    image: ${FPS_REGISTRY:-ghcr.io/robertvejvoda}/fairspot-profile:${FPS_IMAGE_TAG:-latest}
    ports:
      - "5197:5197"
    environment:
      <<: *fairspot-service-auth-env
      ASPNETCORE_URLS: "http://+:5197"
      ASPNETCORE_ENVIRONMENT: ${FPS_ASPNETCORE_ENVIRONMENT:-Production}
    networks:
      - web

  fairspot-reporting:
    image: ${FPS_REGISTRY:-ghcr.io/robertvejvoda}/fairspot-reporting:${FPS_IMAGE_TAG:-latest}
    ports:
      - "5171:5171"
    environment:
      <<: *fairspot-service-auth-env
      ASPNETCORE_URLS: "http://+:5171"
      ASPNETCORE_ENVIRONMENT: ${FPS_ASPNETCORE_ENVIRONMENT:-Production}
      # #763 — Reporting reads durable report data from DataHub over this base URL.
      DataHubService__BaseUrl: "http://fairspot-datahub:5211"
    networks:
      - web

  # Web SPA (nginx). Runtime /config.json is generated from FPS_WEB_* env vars
  # (see code/web/fps-web/docker-entrypoint.sh); without them the baked default
  # is served. Public exposure (Cloudflare hostname) is handled separately.
  fairspot-web:
    image: ${FPS_REGISTRY:-ghcr.io/robertvejvoda}/fairspot-web:${FPS_IMAGE_TAG:-latest}
    ports:
      - "5200:80"
    environment:
      FPS_WEB_API_BASE_URL: ${FPS_WEB_API_BASE_URL:-}
      FPS_WEB_OIDC_AUTHORITY: ${FPS_WEB_OIDC_AUTHORITY:-}
      FPS_WEB_OIDC_CLIENT_ID: ${FPS_WEB_OIDC_CLIENT_ID:-}
      FPS_WEB_OIDC_SCOPES: ${FPS_WEB_OIDC_SCOPES:-openid profile email}
      FPS_WEB_OIDC_REDIRECT_URI: ${FPS_WEB_OIDC_REDIRECT_URI:-}
      FPS_WEB_OIDC_POST_LOGOUT_REDIRECT_URI: ${FPS_WEB_OIDC_POST_LOGOUT_REDIRECT_URI:-}
      FPS_WEB_PRODUCT_NAME: ${FPS_WEB_PRODUCT_NAME:-FairSpot}
      FPS_WEB_TENANT_NAME: ${FPS_WEB_TENANT_NAME:-}
      FPS_WEB_ENVIRONMENT: ${FPS_WEB_ENVIRONMENT:-Production}
    networks:
      - web

networks:
  web:
    external: true
    name: fairspot_network
